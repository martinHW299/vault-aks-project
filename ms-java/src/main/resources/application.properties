server.port=${PORT:8080}

management.endpoints.web.exposure.include=health,info

# Vault connectivity (AppRole)
vault.addr=${VAULT_ADDR:http://vault-server-service.bci-infra:8200}
vault.role-id=${VAULT_ROLE_ID:}
vault.secret-id=${VAULT_SECRET_ID:}
vault.secrets-path=${VAULT_SECRETS_PATH:secret/myapp/test}
